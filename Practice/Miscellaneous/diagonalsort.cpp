#include<bits/stdc++.h>
using namespace std;
#include<vector>

int main()
{
    return 0;
}


class Solution {
public:
    vector<int> deckRevealedIncreasing(vector<int>& deck) {

        sort(deck.begin(), deck.end());
        
    }
};